function squareArray(arr) {
    let squaredArr = [];
    ull;
}

for (let a = 0; a < arr.length; a++) {
    squaredArr.push(arr[a] * arr[a]);
}

return squaredArr;
}

console.log(squareArray([2, 3, 4, 5]));