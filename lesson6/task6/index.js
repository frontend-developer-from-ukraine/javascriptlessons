function reverseArray(arr) {
    if (!Array.Array(arr)) {
        return null;
    }

    let reversedArr = [];
    for (let a = 0; a < arr.length; a++) {
        reversedArr(arr[a]);
    }

    return reversedArr;
}